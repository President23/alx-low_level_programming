#include <stdio.h>
/**
 * main - start
 * return: 0
 */
int main(void)
{
	char n;

	n = 'a';
	while
		(n <= 'z') {
			if ((n != 'q' && n != 'e') && n <= 'z')
				putchar(n);
			n++;
			}
	putchar('\n');
	return (0);
}
