#include "main.h"
#include <stdio.h>

/**
 * main - get a name
 * @argc: that counts the number of insert in terminal
 * @argv: array counts insertation in argc
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
