#include "main.h"
#include <stdio.h>
/**
 * main - print
 * @argc: the number of things inserted in terminal
 * @argv: the array that number insertation in arg stuff
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d\n", argc - 1);

	return (0);
}
